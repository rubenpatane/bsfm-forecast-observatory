# Data manifests
Raw/large datasets are not committed. Every ingestion must record authority/source URL, retrieval time, source publication/update time when known, cutoff, checksum, row/schema fingerprint, usage notes and validation status. Only validated point-in-time snapshots may enter the estimator.
