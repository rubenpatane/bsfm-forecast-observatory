[build-system]
requires=["setuptools>=68"]
build-backend="setuptools.build_meta"
[project]
name="bsfm-forecast-observatory"
version="0.1.0"
requires-python=">=3.11"
dependencies=[]
[project.scripts]
bsfm="bsfm.cli:main"
[tool.pytest.ini_options]
testpaths=["tests"]
